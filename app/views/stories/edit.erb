<div class = "row">
  <form method="POST" action="/stories/<%= @story.id if @story %>" id="create-stories" class="form-horizontal col-md-offset-3 col-md-6">
  <h2 class="center">Edit Story</h2>
  <input type="hidden" name="_form" value="<%= @story.id %>">
    <%= erb :"stories/_form" %>
  </form>
</div>
