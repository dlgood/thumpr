<form method="post" action="/stories" id="create-story">
  <h2>Create a new story</h2>
    <p class="note">The fields in this first section are required</p> 
    <div class="form-group form-inline">
      <label>As a </label>
      <input type="text" name="role" class="form-control role">
      <label>I should be able to </label>
      <input type="text" name="goal" class="form-control goal">  
    </div>
    <div class="form-group form-inline">
      <label>Because </label>
      <input type="text" name="benefit" class="form-control benefit">
    </div>
    <p class="note">The following fields are optional</p>
    <div class="form-group">
      <label>Additional details </label>
      <textarea name="fftext" class="form-control" rows="5">
      (start state) Given 
      (actor action) When I 
      (system feedback) The system
      (acceptance criteria) We will know this is done when we can</textarea>
    </div>
    <div class="select-group">
      <label>Assignee </label>
      <select class="form-control assignee">
        <option disabled selected></option>
        <% @users.each do |user| %>    
          <option name="userid" value="<%= user.id %>"><%= user.name %></option>
        <% end %>
      </select>
    </div>
    <div class="select-group">
      <label>Priority </label>
      <select class="form-control assignee">
        <option disabled selected></option>
      </select>
    </div>
    <input type="submit" value="Create" class="btn btn-primary btn-lg">
</form>
